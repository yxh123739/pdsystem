import 'package:flutter/material.dart';

Text titleText({double fontSize = 20, Color color = Colors.black}) => Text(
      '配电监控系统云端',
      style: TextStyle(
        fontSize: fontSize,
        fontWeight: FontWeight.bold,
        color: color,
      ),
    );
